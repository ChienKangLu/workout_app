import 'package:mockito/annotations.dart';

import '../util/test_callback.dart';

@GenerateMocks([
  TestCallback,
])
void main() {
  /**
   * Generate mock class by running the following command:
   * ```bash
   * flutter pub run build_runner build --delete-conflicting-outputs
   * ```
   */
}
